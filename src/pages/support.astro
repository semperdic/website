---
import BaseLayout from '../layouts/BaseLayout.astro';
import {
  discussionNewUrl,
  githubRepoUrl,
  issueNewUrl,
  SUPPORT_EMAIL,
  withBase,
} from '../lib/github';

const mail = `mailto:${SUPPORT_EMAIL}`;
---

<BaseLayout
  title="Support"
  description="Get help with Semper — manual, community, and email."
  path="/support"
>
  <section class="section" style="padding-top: 3rem">
    <h2>Support</h2>
    <p class="sub">Start with the Manual; use Community for open questions; email for private matters.</p>

    <div style="display: grid; gap: 0.85rem">
      <div class="panel">
        <h3 style="margin: 0 0 0.35rem; font-family: var(--font-display)">Read the Manual</h3>
        <p style="margin: 0 0 0.85rem; color: var(--muted)">
          Step-by-step analysis, exports, and an FAQ with DIC good-practice notes.
        </p>
        <div class="cta-row" style="margin: 0">
          <a class="btn btn-primary" href={withBase('/manual')}>Open Manual</a>
          <a class="btn btn-ghost" href={withBase('/manual/faq')}>FAQ</a>
        </div>
      </div>

      <div class="panel">
        <h3 style="margin: 0 0 0.35rem; font-family: var(--font-display)">Ask the community</h3>
        <p style="margin: 0 0 0.85rem; color: var(--muted)">
          Discussions, bugs, and feature requests — GitHub login required to post.
        </p>
        <div class="cta-row" style="margin: 0">
          <a class="btn btn-primary" href={withBase('/community')}>Community hub</a>
          <a class="btn btn-ghost" href={discussionNewUrl('q-a')} rel="noopener noreferrer">
            New Q&amp;A
          </a>
        </div>
      </div>

      <div class="panel">
        <h3 style="margin: 0 0 0.35rem; font-family: var(--font-display)">Bugs &amp; features</h3>
        <p style="margin: 0 0 0.85rem; color: var(--muted)">Tracked as Issues on GitHub.</p>
        <div class="cta-row" style="margin: 0">
          <a class="btn btn-ghost" href={issueNewUrl('bug_report.yml')} rel="noopener noreferrer">
            Report a bug
          </a>
          <a
            class="btn btn-ghost"
            href={issueNewUrl('feature_request.yml')}
            rel="noopener noreferrer"
          >
            Request a feature
          </a>
        </div>
      </div>

      <div class="panel">
        <h3 style="margin: 0 0 0.35rem; font-family: var(--font-display)">Email support</h3>
        <p style="margin: 0 0 0.85rem; color: var(--muted)">
          Account approval, session limits, or anything you should not post publicly.
        </p>
        <a class="btn btn-primary" href={mail}>{SUPPORT_EMAIL}</a>
      </div>

      <p style="color: var(--muted); font-size: 0.9rem">
        Repository: <a href={githubRepoUrl}>{githubRepoUrl}</a>
      </p>
    </div>
  </section>
</BaseLayout>
