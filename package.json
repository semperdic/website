{
  "name": "semperdic-website",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && firebase deploy --only hosting"
  },
  "dependencies": {
    "astro": "^5.7.10"
  },
  "devDependencies": {
    "firebase-tools": "^14.2.0"
  }
}
